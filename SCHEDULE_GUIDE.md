# 老師時段維護

編輯 `teacher-schedules.js`，再將檔案更新至 GitHub。所有日期與時間均為台灣時間。

老師代號：`amber` 布丁、`neitzu` 念慈、`rex` Rex、`lixinhan` 李昕翰、`inch` 茵淇、`yushan` 宇珊、`yuqing` 郁晴。

尚未填寫時段的新老師會由共用師資資料建立空白排程。要加入真實時段，請在 `window.teacherSchedules` 物件中新增該老師代號、姓名、更新日期與 `dates`；自動初始化不會覆寫已填寫的排程。

每位老師的 `dates` 只填真實的可授課時段。以下為格式範例，請自行替換日期：

```js
updatedAt: "2026-09-07",
dates: {
  "2026-10-01": [
    { start: "10:00", end: "11:00", booked: false },
    { start: "14:00", end: "15:00", booked: true },
  ],
},
```

- `booked: false`：可選擇，並開啟聯絡方式洽詢。
- `booked: true`：已確認預約，時段反黑且不能選。當日所有時段都已預約時，日期也反黑。
- 沒有填寫的日期、空陣列或已過期的時間不開放選取。
- 請將一天拆成可單獨洽詢的時段，不要填重複或互相重疊的區間。若可選時段與已預約時段重疊，系統會封鎖該可選時段。
- 學生開啟聯絡方式不會更改資料。確認預約後，手動將該時段改為 `true`；取消後改回 `false`，並更新 `updatedAt`。
- 不要填學生姓名、電話或任何私人行程，這份檔案會公開於網站。
- 月曆提供本月起 12 個月的日期選擇。沒有未來可洽詢時段時，直接開啟聯絡方式，不顯示空白月曆。

官方 LINE 連結統一設定在 `site-config.js` 的 `lineUrl`。目前為空，按下洽詢會顯示「聯絡方式準備中」，不會傳送訊息。提供官方連結後填入即可套用全站；學生需在 LINE 對話中提供老師、日期及時段，開啟連結不代表已送出或預約成立。
